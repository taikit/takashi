<nav class="navbar navbar-default navbar-fixed-top" style="color: #223a70;  border: 1px solid  #ccc ; height:70px;">
  <div class="container headder_p">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header" >
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <!-- </button> -->
      <%= link_to(root_path, class: "navbar-brand") do %>
        <%= image_tag("logo.png", alt: "triplan", size: "150x37") %>
      <% end %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right" role="menu">
        <li><%= link_to("TOP", root_path) %></li>
        <% if user_signed_in? %>
          <li><%= link_to(current_user.name, users_path) %></li>
          <li><%= link_to 'Sign out', destroy_user_session_path, method: :delete  %></li>
        <% else %>
          <li><%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook) %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
